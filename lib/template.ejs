/*
	Model:		<%= name %>
	Comment:	<%= comment %>
*/

var mongoose = require("mongoose");
var ObjectId = mongoose.Schema.Types.ObjectId;

var <%= name %> = new mongoose.Schema({<% for (var i=0; i<columns.length; i++) { %>
  "<%= columns[i].name %>": { "type": <%= columns[i].type %><% if (columns[i].ref) { %>, "ref": "<%= columns[i].ref %>"<% } %> }<% if (i<columns.length-1) { %>,<% }} %>
});
exports.<%= name %> = mongoose.model("<%= name %>", <%= name %>);